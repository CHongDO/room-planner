/* ============================================================
   main.js — 진입점. 앱 호스트 초기화.
   ============================================================ */

import { initAppHost } from './appHost.js';

document.addEventListener('DOMContentLoaded', () => {
  initAppHost();
});
